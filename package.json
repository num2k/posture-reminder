{
  "name": "posture-reminder",
  "version": "1.0.0",
  "description": "Notification Chrome Extension for Posture Improvement",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "copy:assets": "copyfiles -u 1 package/assets/**/* dist",
    "copy:html": "copyfiles -u 1 package/popup/*.html dist",
    "copy:data": "copyfiles -u 1 package/data/*.json dist",
    "copy:manifest": "copyfiles -u 1 package/manifest.json dist",
    "minify:js": "esbuild package/background.js package/popup/popup.js package/popup/stretching_guide.js package/data/locale-utils.js --bundle --minify --outdir=dist --format=iife --target=es2015",
    "minify:css": "esbuild package/popup/popup.css package/popup/stretching_guide.css --minify --outdir=dist/popup",
    "minify:html": "node minify-html.js",
    "build": "npm run clean && npm run copy:manifest && npm run copy:assets && npm run copy:data && npm run minify:js && npm run minify:css && npm run minify:html",
    "build:prod": "npm run build"
  },
  "author": "num2k",
  "license": "MIT",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "esbuild": "^0.18.20",
    "html-minifier-terser": "^7.2.0",
    "rimraf": "^5.0.1"
  }
}
